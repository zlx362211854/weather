export type Coodinate = {
    lat: number;
    lon: number;
  };
  
  export type Weather = {
      temp: number
      feelsLike: number
      humidity: number
      pressure: number
      windSpeed: number
      description: string
      icon: string
  }